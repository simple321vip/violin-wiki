package com.g.estate.school.concurrent;

/**
 * 1. 关于反编译工具，一个是系统自带的javap -c 命令  这个现在jdk现在起来装起来也挺麻烦的，通常都用idea自带的openjdk就可以了，这样导致javap命令不可用
 * 2. 我们可以通过idea的插件形式来查看字节码。IDEA -> File -> Settings -> Plugins -> search [jclasslib] and install, after that, restart idea
 *      after restart idea you will find a bar of name with [show ByteCode with jclasslib] in the navigator bar [view]
 *
 *
 */
public class FirstClass {
}
